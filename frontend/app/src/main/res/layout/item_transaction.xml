<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:orientation="horizontal"
    android:paddingVertical="12dp"
    android:gravity="center_vertical">

    <!-- Icon Container - poate fi iconiță, inițiale, sau steaguri exchange -->
    <FrameLayout
        android:layout_width="44dp"
        android:layout_height="44dp">

        <!-- Card pentru iconițe (shopping, transfer, etc) -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cardIcon"
            android:layout_width="44dp"
            android:layout_height="44dp"
            app:cardCornerRadius="22dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@color/white_10">

            <ImageView
                android:id="@+id/ivCategoryIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                tools:src="@drawable/ic_shopping"
                app:tint="@color/text_secondary" />

        </androidx.cardview.widget.CardView>

        <!-- Card pentru inițiale (transferuri către persoane) -->
        <androidx.cardview.widget.CardView
            android:id="@+id/cardInitials"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:visibility="gone"
            app:cardCornerRadius="22dp"
            app:cardElevation="0dp"
            app:cardBackgroundColor="@color/green_accent">

            <TextView
                android:id="@+id/tvInitials"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:textColor="@color/text_primary"
                android:textSize="16sp"
                android:textStyle="bold"
                tools:text="LC" />

        </androidx.cardview.widget.CardView>

        <!-- Container pentru exchange (două steaguri suprapuse) -->
        <FrameLayout
            android:id="@+id/exchangeIconContainer"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:visibility="gone">

            <!-- Steagul din spate (moneda sursă) - mai sus și la stânga -->
            <androidx.cardview.widget.CardView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="top|start"
                app:cardCornerRadius="14dp"
                app:cardElevation="2dp">

                <ImageView
                    android:id="@+id/ivFlagFrom"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    tools:src="@drawable/flag_ro" />

            </androidx.cardview.widget.CardView>

            <!-- Steagul din față (moneda destinație) - mai jos și la dreapta -->
            <androidx.cardview.widget.CardView
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_gravity="bottom|end"
                app:cardCornerRadius="14dp"
                app:cardElevation="4dp">

                <ImageView
                    android:id="@+id/ivFlagTo"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    tools:src="@drawable/flag_eur" />

            </androidx.cardview.widget.CardView>

        </FrameLayout>

    </FrameLayout>

    <!-- Transaction Details -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginHorizontal="12dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvMerchantName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/text_primary"
            android:textSize="15sp"
            android:textStyle="bold"
            tools:text="Kaufland" />

        <TextView
            android:id="@+id/tvCategory"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            tools:text="Cumpărături" />

    </LinearLayout>

    <!-- Amount Section -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="end"
        android:orientation="vertical">

        <!-- Primary Amount -->
        <TextView
            android:id="@+id/tvAmount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_primary"
            android:textSize="15sp"
            android:textStyle="bold"
            tools:text="-127,50 lei" />

        <!-- Secondary Amount (pentru exchange) -->
        <TextView
            android:id="@+id/tvSecondaryAmount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            tools:text="+25,00 €" />

        <!-- Time -->
        <TextView
            android:id="@+id/tvTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/text_hint"
            android:textSize="12sp"
            tools:text="14:32" />

    </LinearLayout>

</LinearLayout>
